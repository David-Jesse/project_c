/* A program measuring the length of string */
#include <stdio.h>
#include <string.h>

main()
{
	char str1[] = "It's been a minute since i was romantically involved with someone";

	printf("The length of the string is: %d\n", strlen(str1));
	return 0;
}
