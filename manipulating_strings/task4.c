/* Write a program that uses scanf function to read in two integers */
#include <stdio.h>

main()
{
	int x, y;
	x += y;

	printf("Enter two numbers seperated by a space: \n");
	scanf("%d  %d", &x, &y);
	printf("The sum of the two numbers are: %d\n", x += y);
	return 0;
}

	
