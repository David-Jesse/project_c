/*What is the value of z after the statement*/
#include <stdio.h>

main()
{
	int x, y;

	x = 3;
	y = 6;
	printf("Given x = %d, y = %d\n", x, y);
	printf("z = x + y == 18 produces: %d\n", z = x + y == 18);
	return 0;
}
