/*A program that reads in a character and outputs it using putchar()*/
#include <stdio.h>

main()
{
	putchar(68);
	putchar(10);
	putchar(40);
	putchar(10);
	putchar(80);
	putchar(10);
	putchar(25);
	return 0;
}
